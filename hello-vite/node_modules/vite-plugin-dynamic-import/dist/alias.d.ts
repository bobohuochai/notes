import type { Alias, ResolvedConfig } from 'vite';
export interface AliasReplaced {
    alias: Alias;
    punctuation: null | [string, string];
    importee: string;
    replacedImportee: string;
}
export declare class AliasContext {
    private config;
    private resolve;
    constructor(config: ResolvedConfig, resolve?: import("vite").ResolveFn);
    replaceImportee(importee: string, id: string): Promise<void | AliasReplaced>;
    replaceRawImportee(rawImportee: string, id: string): Promise<void | AliasReplaced>;
    private replace;
}
