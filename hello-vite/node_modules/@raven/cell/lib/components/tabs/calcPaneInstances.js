export default function () {
    var _this = this;
    if (this.$slots.default) {
        var paneSlots = this.$slots.default.filter(function (vnode) {
            return vnode.tag &&
                vnode.componentOptions &&
                vnode.componentOptions.Ctor.options.componentName === 'ElTabPane';
        });
        // update indeed
        var panes = paneSlots.map(function (_a) {
            var componentInstance = _a.componentInstance;
            return componentInstance;
        });
        if (!(panes.length === this.panes.length &&
            panes.every(function (pane, index) { return pane === _this.panes[index]; }))) {
            this.panes = panes;
        }
    }
    else if (this.panes.length !== 0) {
        this.panes = [];
    }
}
